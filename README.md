# GitHub Pages Deployment Workflow

A simple CI/CD project deploying a static website to GitHub Pages using GitHub Actions.

## Requirements
The workflow is triggered only when changes are pushed to the `main` branch affecting `index.html`.
